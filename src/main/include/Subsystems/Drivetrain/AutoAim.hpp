#pragma once

#include "Headers/Headers.hpp"

namespace AimFunctions{
        void determineValues(); // ran to see what the values currently are
        double getForwardSpeed(); // provides speed for the forward direction
        double getSideSpeed(); // provides speed for the sideways direction
        double getRotationSpeed(); // provides speed for rotaion
};
